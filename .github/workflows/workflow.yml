name: workflow
on: [push]
jobs:
  unit-test:
    runs-on: 3.9.6-slim-buster
    steps:
      - uses: actions/checkout@v2
      - run: pip3 install -r requirements.txt
      - run: pip3 install -r tests/requirements.txt
      - run: pytest tests/unit